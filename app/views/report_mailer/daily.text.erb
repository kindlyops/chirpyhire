<%= @report.organization_name %> for <%= @report.humanized_date %>

<%= @report.recipient_first_name %>,

There <%= 'is'.pluralize(@report.qualified_count) %> <%= @report.qualified_count %> new qualified <%= 'candidate'.pluralize(@report.qualified_count) %> waiting for your review. View your candidates <%= link_to 'here', candidates_url << "?stage_name=#{@report.organization.qualified_stage.name}", target: "_blank" %>.

Thanks,

Harry
