doctype html
html
  head
    = render partial: "layouts/head"
  body
    = render "components/guest_navigation"
    div class==(content_for?(:container_class) ? content_for(:container_class) : 'container')
      .row
        main.col
          .mt-3 = render partial: "layouts/alerts"
          = yield
